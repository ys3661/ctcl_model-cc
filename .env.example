# Resend API key used by the feedback form (app/feedback/actions.ts) to email
# submissions. When unset, submissions are logged to the server console instead
# of being emailed, so the form still works in local development.
# Get a key at https://resend.com/api-keys
RESEND_API_KEY=
